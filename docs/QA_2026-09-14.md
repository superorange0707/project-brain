# Project Brain 稳定性、M365 请求与 UI 验证记录

日期：2026-09-14。对象：当前工作区源码。未提交、打包、发布或修改 `dist`。

## 已修复并验证

| 问题 | 现在的行为 | 验证 |
| --- | --- | --- |
| Copilot 第一轮只有纯文本，没有请求 | Continue with AI 提供 **Create request from text**：将用户整理后的代码问题作为 objective，生成并预览有效 JSON；使用当前 ticket 的协议和实际 context ID；点击 Run retrieval 才执行 | JS 交互测试及真实浏览器完成纯文本 → 预览 → 检索 → CTX-001，返回真实源代码证据 |
| M365 示例容易带入假的轮次或 context ID | 第一轮示例使用最小 JSON 请求；不指定 wave，不编造 base_context_id；要求需要代码证据时在同一回复中输出请求 | 模板、协议及生成 kit 测试，包括 Agent Instructions 长度约束 |
| 回复中包含说明、代码围栏或不规则格式时无法识别请求 | 支持说明文字中的 JSON/YAML、围栏、BOM、换行差异；选取最新的结构化请求；错误输入返回可读校验错误 | 格式回归与真实 HTTP 测试，包括错误 version 类型、过深输入、嵌套内容和最新请求损坏 |
| 未安装可选 YAML 解析器时，部分合法 flow 表达式失败 | 改善现有无依赖解析器对引号、逗号、内联映射、列表和注释的处理 | 定向 YAML 回归；不宣称实现完整 YAML 标准 |
| 自动刷新失败后，同一状态永久被禁止重试 | 失败后指数退避，空闲时自动重试；慢操作从实际失败时刻计算退避；检测异常不终止工作线程 | 20 项自动刷新测试，覆盖失败恢复、退避、检测异常和重启 |
| 任务轮询短暂失败或页面刷新后失去结果 | GET 轮询超时后重连，同一个 job ID 继续跟踪；重新加载后恢复后台任务；重试不重发检索 POST | JS 测试；真实服务连续两次注入 503，中途刷新，最终同一 ticket 只有 1 次检索并取得 CTX-001 |
| 非 Git 的已索引快照显示为未就绪 | 本地快照可用时正确显示 ready，同时保留无远端新鲜度检查的信息 | 状态 API 回归和浏览器验证 |
| 长路径撑破结果卡片 | 成功及错误信息允许长文本换行 | 桌面、390px 窄屏及明暗主题检查 |

任务执行期间的草稿保留和重复提交防护也通过交互回归。工作区原本已有其他改动；本记录只归纳本次审查和验证涉及的结果，不将全部现有 diff 归为本次新增。

## 最终验证

使用项目现有 `.venv`，未安装新依赖。

```bash
source .venv/bin/activate
python -m unittest discover -s tests -v
python -m compileall -q brain tests
git diff --check
```

- 完整测试：**843 项，838 通过，5 跳过，0 失败，389.250 秒**。
- 5 项跳过均要求原生 Windows：目录句柄、根目录替换、进程树、跨进程锁、可执行程序路径兼容行为。
- 源码编译和 diff 空白检查通过。
- 完整测试开始后仅增加了重连提示持续显示的 HTML 修正及对应 JS 断言；该最新交互测试另外单独通过。其余被记录的源码文件未变化。
- 完整日志位于临时文件 `/private/tmp/project-brain-final-tests.log`。
- 浏览器使用隔离的临时本地服务和合成代码仓库；测试页及服务已关闭，夹具已清理。未访问企业 M365 会话。

## 性能测量

使用现有测试夹具：4 个临时非 Git 仓库、13 个文件；本地索引，不访问远端。每项先预热一次，再测量 20 次。下表为本地 Python 路径耗时，不是完整浏览器页面的加载时间。

| 操作 | 中位数 | P95 |
| --- | ---: | ---: |
| 请求预览 | 0.29 ms | 0.85 ms |
| 回复分类与预览 | 1.53 ms | 2.81 ms |
| 项目状态 | 72.11 ms | 95.10 ms |
| 已索引关键词查询 | 7.32 ms | 15.99 ms |
| 检索并生成上下文 | 40.35 ms | 51.57 ms |

完整测试中的合成规模检查也通过：100 个微型仓库的刷新约 2.87 秒，检索约 43.86 ms；其中一个仓库变化后的增量刷新约 1.22 秒。它主要验证仓库数量增加时的工作量，不代表 100 个大型生产仓库的实际耗时。没有同条件修改前基线，因此这些数字不作为本次提速比例。

## 使用这次修复

1. 使用包含本次源码修复的 Brain 版本。现有旧版安装包不会自动更新，本次未打包或发布。
2. 在新版的 **Agent kit** 中重新生成 M365 配置，或者运行 `brain agent-kit m365 --json`，替换现有 Agent Builder 中的 Instructions 和相应项目知识文件。无需新建 Agent 或 ticket。
3. Copilot 再次只返回纯文本时，进入 **Continue with AI**，把文字整理成需要回答的代码仓库问题，选择 **Create request from text**，检查预览后运行。该功能使用文字作为检索目标，不会凭空补出文件路径或业务事实。

已验证的是当前源码、合成仓库及本机浏览器的行为。企业真实仓库、实际远端模型/M365 输出和 Windows 本机行为仍需对应环境验证；本次没有确认这些环境永远不会发生错误。
